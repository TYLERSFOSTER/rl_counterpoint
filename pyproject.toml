[project]
name = "rl-counterpoint"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.3",
    "scipy>=1.17.1",
    "torch>=2.11.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
